name: extensions_core
description: "Extensions for dart and flutter"
version: 0.0.3
homepage: https://github.com/LeanQChris/extensions.git

environment:
  sdk: ^3.6.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
